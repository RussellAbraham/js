browser.timing.Interval = function (context, callback, ms, steps){

    if(!this && this instanceof browser.timing.Interval){
        return;
    }

    if(arguments.length.length < 2){
        throw new Error()
    }

} 

browser.timing.Interval.prototype = {}

browser.timing.Interval.forceCall = function(){}

browser.timing.Interval.prototype = {}
